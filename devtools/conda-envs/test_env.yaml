name: test
channels:
  - conda-forge
dependencies:
  # Base depends
  - python
  - pip
  - requests
  - requests-toolbelt

  # Testing
  - black
  - codecov
  - flake8
  - pytest
  - pytest-cov
  - responses

    # Pip-only installs
  - pip:
    # Documentation
    - rinohtype
    - pygments
    - sphinxnotes-strike
    - sphinx-rtd-theme
    - pystemmer
